# Write a lambda function to compute the cube of a number
#  Lambda function are those function which has no name

cube = lambda number: number ** 3  # lambda functions are mainly used in frameworks like django, flask and even in some libraries also.

print(cube(3))